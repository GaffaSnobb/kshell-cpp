#include <vector>
#include "data_structures.hpp"

std::vector<std::vector<unsigned short>> calculate_m_basis_states(const Interaction& interaction, const unsigned short M_target)
{
    std::vector<std::vector<unsigned short>> basis_states;

    return basis_states;
}